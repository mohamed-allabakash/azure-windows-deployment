name: Deploy to Azure Windows

on: [push]

jobs:
  build-and-deploy:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build and deploy
        run: |
          echo "Your deployment steps will go here"